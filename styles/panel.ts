import { StyleSheet } from "react-native";

const panelStyles = StyleSheet.create({
    container: { marginTop: 15, backgroundColor: '#333333' },
    header: { fontSize: 25, textAlign: 'center', color: '#f5f5f5' }
});

export default panelStyles;